<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:colorButtonNormal">@color/primary_dark</item>

    </style>

    <style name="AppTheme.Button.Colored.Large" parent="Widget.AppCompat.Button">
        <!--
            `android:background` overrides the material design drawable which
            causes the button to have square corners in API version < v21.
        -->
        <item name="android:background">@color/primary_dark</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:minHeight">64dip</item>
        <item name="android:minWidth">64dip</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <style name="AppTheme.Button.Large" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/white</item>
        <item name="android:minHeight">64dip</item>
        <item name="android:minWidth">64dip</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <style name="AppTheme.ToggleButton" parent="Base.Widget.AppCompat.Button">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:stateListAnimator">@null</item>
    </style>
</resources>
